Makefile#1

CC=gcc                               променливата СС ще има стойност gcc
src=$(wildcatd *.c)           src  ще бъде равно на списък от всички файлове които завършват на .с 
obj=$(src:.c=.o)            obj ще съдържа всички имена на файлове от src но ще завършват на .о

LDFLAGS=-IGC -lglut -lpng -lz -lm    променлива LDFLAGS ще бъде равна на дясната страна

myprog: $(obj)                      като извикаме make myprog
    $(CC) -o $@ $^ $(LDFLAGS)          ще се компилират с gcc  всички файлове от obj със флагове LDFLAGS
                                    и изходния файл ще се казва myprog

%.o:%.c
	$(CC) -o $@ -c $<         като извикаме make с име на файл който завършва на .о
                                 ще се компилира с gcc файл със същото име като подадено но завършващо на .с
                                  и изходния файл ще се казва като подаденото на make

.PHONY:clean                   това ще работи дори ако има файл на име clean в тек. директория
clean:                         без PHONY таргета clean няма да се изпълнява ако има файл clean
     rm -f $(obj) myprog      
                               Изтриват се файлове myprog и всички файлове от obj тоест файлове завършващи на .o
